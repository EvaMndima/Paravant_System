# PARAVANT Research Layer dependencies (DEC-2026-06-04-001).
#
# These are RESEARCH-ONLY packages. They are NOT installed in the production
# Railway deployment (which uses only requirements.txt). The one-way dependency
# rule (src/ never imports research/) keeps these out of the live system.
#
# Install with:  pip install -r requirements-research.txt
#
# NOTE: The capital-gating DSR math module (research/validation/deflated_sharpe.py)
# deliberately uses ONLY the Python standard library (math.erf for the normal CDF,
# Acklam's algorithm for the inverse) so the instrument that gates real capital
# carries no heavy dependency whose internals could drift across versions. scipy
# below is used by the TESTS for an optional cross-check and by later phases
# (optimization, advanced validation), not by the core DSR computation.

# Base scientific stack (numpy/pandas already in requirements.txt).
scipy>=1.11           # optional DSR cross-check in tests; stats for later phases
statsmodels>=0.14     # time-series + walk-forward statistics (Phase R1+)
scikit-learn>=1.3     # feature analysis, regime classification (Phase R2+)
optuna>=3.4           # Bayesian parameter optimization (Phase R3)
arch>=6.2             # GARCH / tail-risk modeling (later phases)

# Config + serialization.
PyYAML>=6.0           # hypothesis ledger + strategy biographies (canonical store)

# Exploratory + reporting (later phases; not needed for retrospective DSR).
jupyter>=1.0          # notebook environment (conditional Phase R6)
matplotlib>=3.8       # sensitivity plots (Phase R3+)
seaborn>=0.13         # statistical visualization (Phase R3+)
plotly>=5.18          # interactive sensitivity plots (Phase R3+)
