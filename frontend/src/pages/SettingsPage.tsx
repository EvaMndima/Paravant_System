import { PlaceholderPage } from './PlaceholderPage';

const SettingsPage = () => (
  <PlaceholderPage
    title="Settings"
    description="Account preferences, appearance, notifications, and system configuration."
  />
);

export default SettingsPage;
