import { PlaceholderPage } from './PlaceholderPage';

const RiskPage = () => (
  <PlaceholderPage
    title="Risk"
    description="Risk metrics, drawdown analysis, position limits, and exposure management."
  />
);

export default RiskPage;
