[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "PYTHONUNBUFFERED=1 PYTHONPATH=. python scripts/run_paper_trading.py"
healthcheckPath = ""
restartPolicyType = "always"
